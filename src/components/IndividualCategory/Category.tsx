

interface Props {}


{/* <SelectionCard handleDifficultyChange={id => handleDifficultyChange(id)} selectedCategory={selectedCategory} selectedDifficulty={selectedDifficulty} selectedType={selectedType} handleTypeChange={id => handleTypeChange(id)} formValidation={formValidation} handleQuestionCountChange={count => handleQuestionCountChange(count)}></SelectionCard> */}

const Category = (_props: Props) => {
  return (
    <p></p>


  );
};

export default Category;
